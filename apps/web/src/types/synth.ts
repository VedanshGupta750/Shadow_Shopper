// MIRROR of apps/server/src/llm/types.ts (GeneratedFix).
// IMPORTANT: this MUST match the server-side definition exactly.
// If the server changes, update this file in lockstep.

export interface GeneratedFix {
  bullet_rewrites: string[];
  a_plus_suggestion: string;
  image_brief: string;
}
